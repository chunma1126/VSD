public class EnemyRecoveryState : EnemyState
{
    public EnemyRecoveryState(Enemy enemy, EnemyStateMachine stateMachine, string animBoolName) : base(enemy, stateMachine, animBoolName)
    {
        
    }
}